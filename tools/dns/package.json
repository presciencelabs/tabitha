{
  "name": "@tabitha/dns",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Provisions and reconciles DNS records and redirect rules for tabitha.bible's Cloudflare zone",
  "scripts": {
    "check": "tsc --noEmit",
    "check:lint": "eslint .",
    "check:lint:fix": "eslint . --fix",
    "test": "bun run test:unit",
    "test:unit": "bun test",
    "apply": "bun apply.ts"
  },
  "devDependencies": {
    "@tabitha/eslint-config": "workspace:*",
    "@tabitha/tsconfig": "workspace:*",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3"
  }
}
