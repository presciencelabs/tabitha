# CI Test Notes
# Duplicate top-level heading, on purpose

This file exists only to trip `check:md` (markdownlint) as a deliberate, non-blocking
advisory failure while testing the new `pr_summary` PR-comment job. Safe to delete once
the test is done.   
